# دستور المشروع الفني: نظام تتبع توزيع اللقاحات
## (وثيقة مواصفات ملزمة لنموذج الذكاء الاصطناعي المنفّذ)

---

## 0) مقدمة إلزامية للنموذج المنفّذ

أنت مطالب ببناء تطبيق ويب كامل (Full-stack) حسب هذه الوثيقة **حرفيًا**. هذه الوثيقة هي المرجع الوحيد للحقيقة (Single Source of Truth). عند أي تعارض بين فهمك الخاص وهذه الوثيقة، **الوثيقة هي الأصح**. لا تفترض، لا تُبسّط، ولا تحذف أي شرط أمان مذكور هنا حتى لو بدا "غير ضروري".

قبل كتابة أي كود، اقرأ الوثيقة كاملة مرتين. ثم ابدأ بالخطوة الموضحة في القسم 9 (خطة التنفيذ المرحلية) بالترتيب — **لا تقفز بين المراحل**.

---

## 1) نظرة عامة على المشروع

نظام لتتبع سلسلة توزيع اللقاحات:

**السلسلة:** مخازن وزارة الصحة → مندوب الوزارة → مستشفيات الولادة → تسجيل الأطفال الذين تلقوا اللقاح.

**الهدف:** تتبع دقيق ولحظي لعدد اللقاحات المُسلَّمة لكل مستشفى، وعدد اللقاحات المُستخدمة فعليًا (موثّقة ببيانات أطفال حقيقيين)، والرصيد المتبقي — مع منع أي تلاعب أو تعديل بعد التوثيق الرسمي.

**المستخدمون:** عدد المستشفيات في النظام يتراوح بين 4 و10 مستشفيات، بمعدل إجمالي حوالي 600 مولود شهريًا عبر كل المستشفيات مجتمعة.

---

## 1.1) المنصات المحددة إلزاميًا والحزم البرمجية المتوافقة معها

**لا تختر بدائل أو مكتبات مشابهة من عندك — التزم بالتالي حرفيًا لضمان التوافق:**

| الغرض | المنصة | الحزمة/المكتبة الإلزامية | ملاحظة توافق حرجة |
|---|---|---|---|
| قاعدة البيانات + المصادقة + RLS | **Supabase** | `@supabase/supabase-js` (أحدث إصدار) + `@supabase/ssr` للتعامل مع الجلسات في Next.js | **لا تستخدم** الحزمة القديمة `@supabase/auth-helpers-nextjs`، فهي مهجورة رسميًا واستبدلها Supabase بـ `@supabase/ssr` |
| تخزين الصور | ⏸️ **مؤجَّل حاليًا (Phase 2)** — لم يُحسم بعد بين Cloudflare R2 أو Backblaze B2 | `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner` (تصلح لأي من الاثنين لاحقًا لأن كلاهما متوافق مع S3) | **لا تبنِ هذا الجزء الآن**، راجع التنبيه في القسم 4 |
| الواجهة والخادم | **Next.js** (App Router، أحدث إصدار مستقر) + **Tailwind CSS** | — | استخدم Server Components حيث أمكن لتقليل كشف المفاتيح الحساسة في المتصفح |
| الاستضافة والنشر | **Vercel** | — | يُنشر تلقائيًا عند كل `git push` على الفرع الرئيسي بعد الربط بـ GitHub |
| مستودع الكود | **GitHub** | — | اربطه بـ Vercel من البداية (Import Project من GitHub مباشرة) لتفعيل النشر التلقائي |
| إبقاء Supabase نشطًا | **GitHub Actions** (Cron) أو **UptimeRobot** | — | طلب بسيط (GET) لأي endpoint في المشروع كل 48 ساعة كحد أقصى |

**متغيرات البيئة الإلزامية** (تُحفظ في Vercel → Settings → Environment Variables، **ممنوع كتابتها مباشرة داخل ملفات الكود** أو رفعها لـ GitHub):

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=        # يُستخدم في الخادم فقط، أبدًا في كود المتصفح
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
```

أضف ملف `.env.example` بنفس هذه الأسماء (بدون قيم حقيقية) في المستودع، وأضف `.env.local` إلى `.gitignore` فورًا قبل أول Commit، لمنع تسريب أي مفتاح حساس على GitHub بالخطأ.

---

## 2) الأدوار الأربعة وصلاحياتها (الأهم في المشروع)

يجب تطبيق هذه الأدوار عبر **Row Level Security (RLS)** في قاعدة البيانات، وليس فقط عبر إخفاء أزرار في الواجهة. إخفاء الأزرار وحده **غير كافٍ ويُعتبر ثغرة أمنية**.

| # | الدور | ينتمي لـ | الصلاحيات | ملاحظات حرجة |
|---|---|---|---|---|
| 1 | `hospital_entry` (مُدخل بيانات) | مستشفى واحد محدد | إضافة/تعديل سجلات الأطفال والتطعيمات الخاصة **بمستشفاه فقط**، طالما لم تُوثَّق | لا يرى بيانات مستشفيات أخرى إطلاقًا |
| 2 | `hospital_verifier` (الموثّق/المراجع) | مستشفى واحد محدد | يراجع سجلات مستشفاه ويضغط "توثيق"، بعدها **تُقفل السجلات من التعديل نهائيًا** لأي حساب من نوع `hospital_entry` أو `hospital_verifier` لنفس المستشفى | التوثيق عملية لا رجعة فيها من واجهة المستخدم العادية |
| 3 | `moh_level1` (وزارة - مستوى أول) | مستشفى واحد أو أكثر (Many-to-many) | يُدخل كميات اللقاحات المُسلَّمة لكل مستشفى (شحنات)، ويراجع تقارير المستشفيات المرتبطة به فقط | إذا مستخدم واحد مرتبط بأكثر من مستشفى، يجب أن يرى بيانات كل المستشفيات المرتبطة به فقط، لا كل النظام |
| 4 | `moh_admin` (الإدارة العليا) | كل النظام | عرض ومراقبة فقط (Read-only) لكل المستشفيات وكل الشحنات وكل السجلات، بدون أي تعديل | ⚠️ هذا الدور **يجب ألا يملك صلاحية UPDATE أو DELETE على أي جدول بيانات عملياتي**، حتى على مستوى قاعدة البيانات |

**قاعدة عزل صارمة (Multi-tenancy):** كل استعلام لأي حساب من نوع hospital_* يجب أن يُفلتر تلقائيًا بـ `hospital_id` الخاص به عبر RLS policy، وليس عبر شرط في كود الواجهة الأمامية. اختبر هذا صراحة: حاول من حساب مستشفى A الوصول لبيانات مستشفى B عبر التلاعب المباشر بالـ API — يجب أن يفشل الطلب.

---

## 3) نموذج قاعدة البيانات (Schema) — PostgreSQL عبر Supabase

### الجداول الأساسية:

**`hospitals`**
- `id` (uuid, PK)
- `name` (text)
- `created_at`

**`user_profiles`** (يربط مستخدمي Supabase Auth بالأدوار)
- `id` (uuid, PK, = auth.users.id)
- `role` (enum: `hospital_entry`, `hospital_verifier`, `moh_level1`, `moh_admin`)
- `full_name` (text)

**`user_hospital_links`** (جدول ربط many-to-many بين المستخدمين والمستشفيات — ضروري لأن مستخدم moh_level1 قد يُربط بأكثر من مستشفى)
- `user_id` (FK → user_profiles)
- `hospital_id` (FK → hospitals)

**`warehouse_shipments`** (شحنات المخزن للمندوب — اختياري حسب الحاجة لاحقًا، أو تُدخل يدويًا كرقم إجمالي)
- `id`, `total_quantity`, `received_by`, `received_at`

**`vaccine_batches`** (دفعات اللقاح المُسلَّمة للمستشفى — تسمح بوجود عدة دفعات نشطة في نفس الوقت)
- `id` (uuid, PK)
- `hospital_id` (FK)
- `quantity` (integer) — عدد اللقاحات في هذه الدفعة تحديدًا
- `delivery_date` (date) — تاريخ دخول الدفعة إلى المستشفى
- `batch_number` (text) — رقم التشغيلة (Lot Number)
- `expiry_date` (date) — تاريخ صلاحية هذه الدفعة تحديدًا
- `created_by` (FK → user_profiles، يجب أن يكون moh_level1)
- `notes` (text, nullable)
- `created_at`

**`vaccinators`** (قائمة القائمين بالتطعيم داخل كل مستشفى — يديرها الموثّق فقط)
- `id` (uuid, PK)
- `hospital_id` (FK)
- `full_name` (text)
- `is_active` (boolean, default true) — للإيقاف دون حذف السجل التاريخي
- `added_by` (FK → user_profiles، يجب أن يكون hospital_verifier)
- `created_at`

**`child_vaccination_records`** (سجلات الأطفال — الجدول الأهم)
- `id` (uuid, PK)
- `hospital_id` (FK) — **إلزامي في كل صف، هذا هو أساس عزل البيانات**

*بيانات الطفل:*
- `child_full_name` (text)
- `child_gender` (enum: male/female)
- `birth_date` (date)
- `child_nationality` (text)

*بيانات الأب (قسم مستقل في نموذج الإدخال، حقول منفصلة):*
- `father_first_name` (text) — اسم الأب
- `father_grandfather_name` (text) — اسم الجد
- `father_national_id` (text)
- `father_passport_number` (text, nullable)
- `father_id_image_key` (text) — **ليس الصورة نفسها، بل مفتاح/مسار الملف في R2**

*بيانات الأم (قسم مستقل مماثل):*
- `mother_first_name` (text)
- `mother_grandfather_name` (text)
- `mother_national_id` (text)
- `mother_passport_number` (text, nullable)
- `mother_id_image_key` (text)

*بيانات التطعيم:*
- `vaccination_date` (date)
- `batch_id` (FK → vaccine_batches) — **الدفعة المحددة التي استُخدمت لهذا الطفل، يختارها المُدخل يدويًا عند وجود أكثر من دفعة نشطة**
- `vaccinator_id` (FK → vaccinators) — القائم بالتطعيم، يُختار من القائمة التي يديرها الموثّق فقط (لا إدخال حر)

*تتبّع وتوثيق:*
- `entered_by` (FK → user_profiles)
- `is_verified` (boolean, default false)
- `verified_by` (FK → user_profiles, nullable)
- `verified_at` (timestamp, nullable)
- `created_at`, `updated_at`

**ملاحظة عرض مهمة (تنطبق على كل الشاشات والتقارير عدا نموذج الإدخال نفسه):** `father_first_name` و`father_grandfather_name` يُدمجان ويُعرضان كاسم واحد متصل (مثال: "أحمد محمد")، وكذلك اسم الأم — لا تُعرض كحقول منفصلة مبعثرة إلا في نموذج الإدخال والتعديل فقط.

**`audit_log`** (سجل تدقيق — إلزامي لكل عملية حساسة)
- `id`, `table_name`, `record_id`, `action` (insert/update/verify/delete_attempt), `performed_by`, `performed_at`, `old_value` (jsonb, nullable), `new_value` (jsonb, nullable)

### القيود الإلزامية على مستوى قاعدة البيانات (وليس فقط في الواجهة):

1. **Trigger منع التعديل بعد التوثيق:** أنشئ `BEFORE UPDATE trigger` على `child_vaccination_records` يرفض أي تحديث إذا كان `is_verified = true` في الصف القديم، **إلا** إذا كان المستخدم المنفّذ من دور `moh_admin` وكان التحديث تصحيحًا موثقًا يُسجَّل إجباريًا في `audit_log`. هذا يمنع أي التفاف على القيد من الواجهة الأمامية.
2. **لا تسمح بحساب الرصيد يدويًا في الواجهة فقط** — احسب الرصيد المتبقي دائمًا عبر Query أو View في قاعدة البيانات، على مستويين:
   - **رصيد كل دفعة على حدة**: `رصيد_الدفعة = vaccine_batches.quantity - COUNT(child_vaccination_records WHERE batch_id = X)`
   - **الرصيد الإجمالي للمستشفى**: مجموع أرصدة كل دفعاته النشطة معًا
   أنشئ `VIEW` باسم `batch_balance_view` يحسب رصيد كل دفعة تلقائيًا، ولا تخزّن أي رصيد كعمود ثابت (لتجنب تضارب البيانات).
3. **عند تسجيل تطعيم طفل ووجود أكثر من دفعة برصيد متبقٍ > 0**: اعرض للمُدخل قائمة اختيار (dropdown) بكل الدفعات المتاحة (رقم التشغيلة + الرصيد المتبقي لكل منها) ليختار منها يدويًا — لا تختر تلقائيًا.
4. **تحقق من تاريخ الصلاحية**: عند اختيار دفعة، تحقق أن `vaccination_date` <= `expiry_date` الخاص بها، وإن لم يكن كذلك اعرض تحذيرًا واضحًا (دفعة منتهية الصلاحية) قبل السماح بالمتابعة.
5. **امنع الرصيد السالب منطقيًا**: عند إدخال سجل طفل جديد لدفعة معينة، تحقق أولًا أن رصيد تلك الدفعة تحديدًا > 0، وإن لم يكن كذلك اعرض تحذيرًا واضحًا للمستخدم قبل الحفظ (وليس منعًا صامتًا).

---

## 4) الصور (هويات الأب والأم) — قواعد أمان صارمة

⚠️ **تنبيه للمرحلة الحالية: هذا القسم مؤجَّل مؤقتًا (Phase 2)، لا تنفّذه الآن.** السبب: لم يُحسم بعد اختيار مزود التخزين (Cloudflare R2 أو Backblaze B2 أو غيره). **تجاهل كل ما في هذا القسم حاليًا وتابع بناء باقي النظام بدونه بالكامل.** سيُستكمل هذا الجزء لاحقًا كإضافة منفصلة لا تؤثر على أي جزء آخر من الكود إن اتُّبعت قواعد فصل الطبقات في القسم 11.

**التعديل المؤقت المطلوب على الجداول الآن:** أبقِ عمودي `father_id_image_key` و`mother_id_image_key` في جدول `child_vaccination_records` موجودين في التصميم لكن اجعلهما **قابلين لأن يكونا فارغين (nullable)**، ولا تبنِ أي واجهة رفع صور أو منطق R2/S3 الآن. كل شاشات الإدخال والتقارير تعمل بدون هذين الحقلين مؤقتًا (تُترك فارغة).

**عندما نحسم مزود التخزين لاحقًا، سيُطبَّق التالي (للرجوع إليه وقتها فقط):**
1. الصور **لا تُخزَّن أبدًا في قاعدة البيانات نفسها**، فقط في خدمة تخزين ملفات خارجية. قاعدة البيانات تخزن فقط الـ `key` (المسار).
2. **لا تجعل bucket الصور عامًا (public)**. يجب أن يكون كل وصول للصورة عبر **Signed URL مؤقت** (صلاحية دقائق قليلة فقط)، يُنشأ من الخادم بعد التحقق من صلاحية المستخدم (هل له حق رؤية هذا الطفل حسب `hospital_id`؟).
3. اضغط الصور تلقائيًا قبل الرفع النهائي إلى حجم معقول (حوالي 300-500 كيلوبايت، أبعاد لا تتجاوز 1500 بكسل) — هذا يوفر التخزين ويُسرّع الرفع، دون أن يفقد وضوح القراءة.
4. لا تسمح برفع أي نوع ملف غير صورة (تحقق من `MIME type` فعليًا على الخادم، وليس فقط امتداد اسم الملف).

---

## 5) المصادقة والصلاحيات التقنية (Supabase Auth + RLS)

- استخدم Supabase Auth القياسي (بريد إلكتروني + كلمة مرور كحد أدنى).
- **لا تستخدم أبدًا الـ `service_role key` في كود الواجهة الأمامية (Frontend/Client-side)** — هذا مفتاح يتجاوز كل قيود RLS بالكامل، ويجب أن يبقى فقط في بيئة الخادم (Server-side / Edge Functions) إن استُخدم أصلًا.
- كل جدول عملياتي (`hospital_deliveries`, `child_vaccination_records`) يجب أن يكون عليه `RLS ENABLED` منذ اللحظة الأولى لإنشائه، وليس كخطوة لاحقة.
- اكتب RLS Policy منفصلة لكل عملية (SELECT, INSERT, UPDATE) لكل دور — لا تكتفِ بسياسة عامة واحدة فضفاضة.

---

## 6) الواجهة (Frontend) — المتطلبات

- التطبيق باللغة العربية بالكامل، اتجاه **RTL** إلزامي في كل الصفحات.
- 4 لوحات تحكم منفصلة حسب الدور (لا تعرض لوحة واحدة بها كل شيء ثم تُخفي عناصر — كل دور له مسار/صفحة مخصصة).
- شاشة تسجيل الدخول موحّدة، وتوجيه المستخدم تلقائيًا للوحته حسب دوره بعد الدخول.
- عرض رسائل تحذير واضحة (مثل: "الرصيد المتبقي X فقط، هل أنت متأكد؟") قبل أي عملية حساسة.
- التقنية المقترحة: **Next.js (React)** + Tailwind CSS، مستضاف على Vercel.

**نموذج تسجيل بيانات الطفل — بنية إلزامية:**
- **قسم بيانات الطفل**: حقل/خلية منفصلة لكل بيان — اسم الطفل، النوع (ذكر/أنثى)، تاريخ الميلاد (عبر منتقي تاريخ Date Picker يُفتح بزر أيقونة صغير بجانب الحقل، وليس إدخال نص حر)، الجنسية.
- **قسم مستقل بصريًا (منفصل عن قسم الطفل) لبيانات الأب**: اسم الأب، اسم الجد، الرقم القومي، رقم جواز السفر (اختياري) — كل بيان في حقل منفصل.
- **قسم مستقل آخر مماثل لبيانات الأم**: نفس البنية (اسم الأم، اسم الجد للأم، الرقم القومي، رقم الجواز).
- **قسم اختيار دفعة اللقاح**: قائمة منسدلة تعرض كل دفعة متاحة (رقم التشغيلة + الرصيد المتبقي)، يختار المُدخل منها يدويًا (راجع القسم 3).
- **قسم اختيار القائم بالتطعيم**: قائمة منسدلة من جدول `vaccinators` فقط (لا كتابة حرة).
- ⚠️ هذا التقسيم لحقول الإدخال **لا ينطبق على شاشات العرض والتقارير** — هناك تُعرض الأسماء مدمجة كاملة (راجع ملاحظة العرض في القسم 3).

**إدارة قائمة القائمين بالتطعيم:** شاشة مخصصة تظهر فقط لحساب `hospital_verifier`، تسمح بإضافة اسم جديد للقائمة أو إيقاف اسم موجود (`is_active = false`) دون حذفه نهائيًا (للحفاظ على السجل التاريخي لأي طفل سُجِّل باسمه سابقًا).

---

## 7) نقاط الخطأ والالتباس المتوقعة (يجب الانتباه لها تحديدًا)

هذه أخطاء شائعة تحدث عند بناء أنظمة مشابهة — تجنبها صراحة:

1. **الخلط بين "التسليم" و"الاستخدام":** الشحنة المُسلَّمة للمستشفى (`hospital_deliveries`) شيء، وعدد الأطفال المُلقَّحين فعليًا (`child_vaccination_records`) شيء آخر. لا تدمجهما في جدول واحد أبدًا.
2. **حساب الرصيد بشكل تراكمي خاطئ عبر عدة دفعات:** إذا استلمت المستشفى 3 دفعات في أوقات مختلفة، يجب أن يكون الرصيد = مجموع كل الدفعات - كل الاستخدام التراكمي منذ البداية، وليس دفعة بدفعة منفصلة.
3. **السماح بالتعديل بعد التوثيق عبر "ثغرة" في الواجهة:** تأكد أن القيد مطبّق في قاعدة البيانات (Trigger) وليس فقط بإخفاء زر "تعديل" في React — أي مستخدم تقني بسيط يقدر يستدعي الـ API مباشرة ويتجاوز واجهة الموقع.
4. **تسريب صور الهويات عبر روابط عامة دائمة:** لو استخدمت رابط دائم غير موقّع (unsigned public URL) لصورة في R2، فأي شخص يملك الرابط يقدر يفتحه للأبد بدون تسجيل دخول. يجب Signed URL مؤقت فقط.
5. **عدم عزل بيانات المستشفيات عن بعضها (Tenant Isolation):** اختبر تحديدًا أن حساب مستشفى A لا يقدر يشوف أو يعدّل بيانات مستشفى B، حتى لو حاول تخمين الـ `id` مباشرة عبر الـ API.
6. **تعطل النظام بسبب "نوم" Supabase المجاني بعد 7 أيام خمول:** أضف آلية Health-check دورية (خارجية، عبر UptimeRobot أو GitHub Actions Cron) تستدعي الـ API كل يوم أو يومين للإبقاء على المشروع نشطًا دومًا.
7. **الخلط بين المنطقة الزمنية:** خزّن كل التواريخ بصيغة UTC في قاعدة البيانات، واعرضها للمستخدم محوّلة للتوقيت المحلي في الواجهة فقط — لا تخزّن وقتًا محليًا مباشرة.
8. **حذف سجلات موثقة عن طريق الخطأ:** لا تفعّل صلاحية DELETE الفعلية على `child_vaccination_records` لأي دور إطلاقًا بعد التوثيق. استخدم Soft Delete (عمود `is_deleted`) إن احتجت الإخفاء، مع تسجيل ذلك في `audit_log`.
9. **تعديل يكسر باقي المشروع:** عند إجراء أي تعديل لاحق على الكود، لا تُعِد كتابة ملفات كاملة من الصفر إن لم يكن ضروريًا — عدّل فقط الجزء المطلوب، واختبر بعدها أن باقي الوظائف (خصوصًا RLS وحسابات الرصيد) لم تتأثر.

---

## 8) الاختبارات الإلزامية قبل اعتبار أي مرحلة "منتهية"

لكل ميزة تُبنى، نفّذ هذه الاختبارات قبل الانتقال للميزة التالية:

- [ ] تسجيل الدخول بكل نوع حساب من الأربعة، والتأكد من التوجيه الصحيح للوحته
- [ ] محاولة وصول حساب مستشفى A لبيانات مستشفى B عبر تعديل الرابط/الطلب مباشرة → يجب أن يُرفض
- [ ] محاولة تعديل سجل طفل بعد توثيقه من نفس حساب المستشفى → يجب أن يُرفض من قاعدة البيانات
- [ ] التأكد أن حساب `moh_admin` لا يقدر يعدّل أو يحذف أي شيء، فقط يعرض
- [ ] ~~رفع صورة هوية والتأكد أنها لا تُفتح إلا برابط موقّت موقّع~~ (مؤجَّل لمرحلة الصور — القسم 4)
- [ ] إدخال دفعة تسليم جديدة، ثم إدخال أطفال، والتأكد أن الرصيد المعروض صحيح حسابيًا
- [ ] محاولة تسجيل طفل والرصيد = صفر → يجب ظهور تحذير واضح
- [ ] فحص أن كل عملية حساسة (توثيق، تعديل، تسليم) سُجّلت في `audit_log`
- [ ] إنشاء دفعتين لنفس المستشفى، والتأكد أن المُدخل يقدر يختار من أيهما يسجّل، وأن رصيد كل دفعة يُحدَّث بشكل مستقل وصحيح
- [ ] محاولة تسجيل طفل من دفعة منتهية الصلاحية → يجب ظهور تحذير واضح
- [ ] التأكد أن حساب `hospital_entry` لا يقدر يضيف اسمًا جديدًا لقائمة القائمين بالتطعيم، وأن هذه الصلاحية حصرية لـ `hospital_verifier`

---

## 9) التقارير والبحث (لكل الأدوار الأربعة، ضمن نطاق صلاحياتها)

**قاعدة أساسية:** أي تقرير أو بحث يجب أن يمرّ عبر نفس RLS المطبّق على الجداول — حساب مستشفى A يستخرج تقارير عن بياناته فقط، ولا يقدر يرى بيانات مستشفى B حتى في شاشة التقارير. لا تكتب استعلام تقارير منفصل يتجاوز RLS "للتبسيط".

**المتطلبات:**
1. **فلترة بنطاق تاريخ (من - إلى):** تُطبَّق على `vaccination_date` أو `birth_date` أو `delivered_at` حسب نوع التقرير، عبر Query params في الخادم (لا تُحمَّل كل البيانات للمتصفح ثم تُفلتر محليًا — استعلم من قاعدة البيانات مباشرة بالنطاق).
2. **إحصائيات الجنس:** عرض عدد الذكور، عدد الإناث، والإجمالي ضمن النطاق المحدد (استخدم `COUNT(*) FILTER (WHERE gender = 'male')` في PostgreSQL أو ما يعادلها).
3. **بحث نصي حر:** بحث عبر كل الحقول النصية ذات الصلة (اسم الطفل، ملاحظات، اسم المستشفى) — استخدم `ILIKE '%query%'` كحل بسيط وكافٍ لحجم البيانات المتوقع (لا حاجة لمحرك بحث خارجي معقد).
4. **الطباعة/التصدير:** يُنفَّذ بطريقتين مقبولتين:
   - طباعة مباشرة من المتصفح عبر تنسيق CSS مخصص لـ `@media print` (الأبسط والأسرع تنفيذًا)
   - أو توليد PDF قابل للتحميل (اختياري، أعلى تعقيدًا) عبر مكتبة مثل `@react-pdf/renderer`

**الحقول الإلزامية في تقرير/سجل الطفل المطبوع** (الأسماء تُعرض مدمجة كاسم كامل، وليست حقولًا مبعثرة — راجع ملاحظة العرض في القسم 3):
   - اسم الأب الكامل + الرقم القومي للأب
   - اسم الأم الكامل + الرقم القومي للأم
   - تاريخ ميلاد الطفل
   - تاريخ التطعيم
   - اسم القائم بالتطعيم (من جدول `vaccinators`)
   - تاريخ دخول الدفعة (`vaccine_batches.delivery_date`) التي استُخدمت في تطعيم هذا الطفل تحديدًا (عبر `batch_id`)
5. كل تقرير يُنشأ يجب تسجيله في `audit_log` (من طلبه، ونطاق البيانات الذي طلبه) لأن هذه بيانات حساسة تخص أطفالًا.

---

## 10) التحقق من صحة الرقم القومي المصري (لأرقام الأب والأم)

⚠️ **تنبيه إلزامي قبل التنفيذ:** الخانة الأخيرة (رقم 14) من الرقم القومي المصري هي **رقم تحقق داخلي خاص بوزارة الداخلية غير منشور رسميًا**، ولا توجد معادلة رياضية موثوقة يمكن لأي نظام خارجي حسابها والتحقق منها بشكل مستقل. **ممنوع اختراع أو استخدام أي "معادلة" منتشرة على الإنترنت لهذه الخانة** — فهذا سيؤدي لرفض أرقام صحيحة أو قبول أرقام خاطئة.

**لذلك، التحقق يقتصر على الأجزاء القابلة للتحقق فعليًا (وهي كافية تمامًا لحل مشكلة المستخدم):**

بنية الرقم (14 خانة رقمية بالترتيب من اليسار):
| الخانات | تمثل | التحقق المطلوب |
|---|---|---|
| 1 | قرن الميلاد | يجب أن تكون `2` (مواليد 1900-1999) أو `3` (مواليد 2000-2099) فقط |
| 2-3 | سنة الميلاد (آخر رقمين) | أي قيمة 00-99 مقبولة |
| 4-5 | الشهر | يجب أن تكون بين 01 و12 |
| 6-7 | اليوم | يجب أن يكون يومًا صحيحًا فعليًا لهذا الشهر/السنة (مع مراعاة السنوات الكبيسة لشهر فبراير) |
| 8-9 | كود المحافظة | فحص ثانوي غير حاسم (تنبيه وليس رفضًا): يُفضَّل أن يكون ضمن القائمة الشائعة `01,02,03,04,11,12,13,14,15,16,17,18,19,21,22,23,24,25,26,27,28,29,31,32,33,34,35,88` (تم التحقق من أول 4 أكواد منها بشكل مستقل: القاهرة=01، الإسكندرية=02، بورسعيد=03، السويس=04؛ باقي القائمة غير مؤكد بنفس القوة، لذا **لا تُستخدم لرفض الحفظ نهائيًا**، فقط كتنبيه اختياري) |
| 10-12 | تسلسل يومي | لا يحتاج تحققًا خاصًا |
| **13** | **الجنس** | **فردي (1,3,5,7,9) = ذكر، زوجي (2,4,6,8) = أنثى — هذا هو الفحص الأهم والمطلوب فعليًا** |
| 14 | رقم تحقق داخلي (وزارة الداخلية) | **لا يُتحقق منه برمجيًا — غير قابل للحساب المستقل** |

**منطق التنبيه المطلوب تحديدًا (حل مشكلة "ذكر بدل أنثى"):**

عند إدخال الرقم القومي لأي من الأب أو الأم في نموذج تسجيل الطفل:
1. استخرج رقم الخانة 13 وحدد الجنس المتوقع منه (ذكر/أنثى).
2. إن كان النظام يحتوي حقلًا منفصلاً يوضح جنس صاحب الرقم (مثلاً حقل ضمني: "هذا رقم الأب" يُفترض ذكر، "هذا رقم الأم" يُفترض أنثى)، قارن الجنس المستخرج من الرقم القومي بالجنس المتوقع حسب الحقل (أب/أم).
3. عند التعارض (مثال: رقم مُدخل في خانة "الأم" لكن الخانة 13 فردية = ذكر)، اعرض تحذيرًا واضحًا وفوريًا للمستخدم: "هذا الرقم القومي يشير إلى شخص ذكر، بينما الحقل المُدخل هو رقم الأم — يرجى التأكد من الرقم" — **دون منع الحفظ نهائيًا** (فقد يكون هناك حالات استثنائية حقيقية)، فقط تنبيه يتطلب تأكيدًا واعيًا من المستخدم قبل المتابعة.
4. اعرض نفس منطق التحقق من الصلاحية (14 خانة، القرن، الشهر، اليوم) فورًا أثناء الكتابة (Client-side validation) لتجربة استخدام أسرع، **مع إعادة نفس التحقق إلزاميًا على الخادم أيضًا** قبل الحفظ في قاعدة البيانات (لا تكتفِ بالتحقق في المتصفح فقط، فيمكن تجاوزه بسهولة).

---

## 12) معايير تنظيم الكود (إلزامية — لضمان قابلية الصيانة والتعديل لاحقًا)

المشروع سيُعدَّل ويُضاف إليه باستمرار من قبل أشخاص أو نماذج ذكاء اصطناعي مختلفة عبر الزمن. لذلك **بنية الكود لا تقل أهمية عن وظائفه**. التزم بالتالي حرفيًا:

**1) فصل الطبقات (Separation of Concerns) — لا تخلط أبدًا:**
- منطق الوصول لقاعدة البيانات (queries) في ملفات/دوال منفصلة تمامًا عن مكوّنات الواجهة (UI components)
- منطق التحقق من الصحة (مثل التحقق من الرقم القومي، حساب الرصيد) في دوال مستقلة قابلة للاستدعاء والاختبار بمعزل عن أي واجهة
- لا تكتب استعلام SQL مباشر داخل مكوّن React — استدعِ دالة من طبقة وسيطة (`/lib/db/*.ts` مثلًا)

**2) هيكل مجلدات ثابت ومتوقع** (مثال إلزامي لتطبيق Next.js):
```
/app                    → الصفحات والمسارات فقط (توجيه، لا منطق أعمال)
/components             → مكوّنات واجهة قابلة لإعادة الاستخدام، كل مكوّن في ملفه الخاص
/lib/db                 → كل دوال التعامل مع Supabase (كل جدول/مجال له ملف مستقل: hospitals.ts, children.ts, deliveries.ts)
/lib/validation          → دوال التحقق (رقم قومي، تواريخ، إلخ) — منفصلة تمامًا وقابلة للاختبار وحدها
/lib/storage             → دوال رفع/جلب الصور من R2
/lib/reports             → منطق التقارير والإحصائيات
/types                   → تعريفات الأنواع (TypeScript) المشتركة بين كل الملفات
```

**3) قاعدة "دالة واحدة، مسؤولية واحدة":**
كل دالة تفعل شيئًا واحدًا محددًا فقط. إذا وجدت دالة تتجاوز 40-50 سطرًا، قسّمها لدوال أصغر بأسماء واضحة تشرح الغرض.

**4) التعديل الآمن — عند إضافة ميزة جديدة أو تعديل موجودة:**
- لا تُعِد كتابة ملف كامل من الصفر إن كان التعديل جزئيًا — عدّل فقط الجزء المطلوب
- لا تُغيّر توقيع دالة مستخدمة في أكثر من مكان (parameters/return type) دون تحديث كل الأماكن التي تستدعيها
- قبل أي تعديل على منطق حساس (حساب الرصيد، RLS، التحقق من الرقم القومي)، تأكد من قراءة اختبارات القسم 8 ذات الصلة وتشغيلها بعد التعديل
- استخدم أسماء دوال ومتغيرات واضحة تشرح نفسها بذاتها (مثل `calculateRemainingVaccineBalance` وليس `calc` أو `fn1`)

**5) توثيق داخل الكود (Comments):**
كل دالة تحتوي منطقًا غير بديهي (خصوصًا: حساب الرصيد، التحقق من الرقم القومي، RLS policies، الـ triggers) يجب أن تحتوي تعليقًا قصيرًا يشرح **لماذا** (وليس فقط ماذا) — مثال: `// نتحقق من is_verified هنا لمنع التعديل بعد التوثيق حسب القسم 3 من المواصفات`.

**6) عدم التكرار (DRY):**
أي منطق يُستخدم في أكثر من مكان (مثل دالة التحقق من الرقم القومي، أو تنسيق التاريخ) يُكتب مرة واحدة في `/lib` ويُستورد، لا يُنسخ ويُلصق في كل ملف.

**7) قاعدة بيانات كمصدر وحيد للحقيقة:**
كما ذُكر في القسم 3، لا تُحسب أرقام مثل الرصيد المتبقي في كود الواجهة الأمامية بمنطق منفصل قد يتعارض مع منطق مشابه في مكان آخر — احسبها دائمًا عبر الـ View أو دالة واحدة مركزية في `/lib/db`، ونادِ عليها من كل مكان تحتاجها.

---

## 13) خطة التنفيذ المرحلية (نفّذها بالترتيب، لا تتخطَّ مرحلة)

1. إنشاء مشروع Supabase + تفعيل RLS من البداية على كل جدول فور إنشائه
2. بناء الجداول والعلاقات (القسم 3) + الـ Triggers والـ View — اترك عمودي صور الهويات فارغين (nullable) كما ذُكر في القسم 4
3. بناء نظام المصادقة والأدوار الأربعة + اختبار العزل بين المستشفيات (القسم 8) قبل أي شيء آخر
4. بناء لوحة `hospital_entry` (إدخال بيانات الأطفال) — بدون أي رفع صور حاليًا
5. بناء لوحة `hospital_verifier` (التوثيق والقفل)
6. بناء لوحة `moh_level1` (إدخال الشحنات ومراجعة التقارير)
7. بناء لوحة `moh_admin` (عرض ومراقبة فقط)
8. إضافة دالة التحقق من الرقم القومي (القسم 10) في نموذج تسجيل الطفل، مع تكرارها على الخادم إلزاميًا
9. بناء شاشات التقارير والبحث (القسم 9) لكل الأدوار الأربعة ضمن نطاق صلاحياتها
10. نشر على Vercel + ربط النطاق + إعداد Health-check الدوري لـ Supabase
11. تنفيذ كل الاختبارات في القسم 8 قبل التسليم النهائي
12. **(اختياري لاحقًا، منفصل تمامًا)**: بعد حسم مزود تخزين الصور (R2 أو Backblaze B2)، نفّذ القسم 4 كاملًا كإضافة مستقلة

---

**تذكير أخير للنموذج المنفّذ:** أمان بيانات الأطفال وهويات ذويهم أولوية قصوى تفوق أي اعتبار آخر (سرعة، بساطة كود، تقليل عدد الأسطر). أي اختصار يُضعف العزل بين المستشفيات أو يسمح بكشف صور الهويات دون تفويض هو خطأ غير مقبول مهما كانت الأسباب.
