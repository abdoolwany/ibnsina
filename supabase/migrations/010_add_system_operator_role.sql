-- ============================================================
-- الترحيل 10: إضافة الدور الخامس (مشغل النظام/التخزين)
-- يُنفذ وحده لأن القيمة الجديدة للـ enum لا تُستخدم في نفس المعاملة
-- ============================================================
ALTER TYPE user_role ADD VALUE IF NOT EXISTS 'system_operator';
